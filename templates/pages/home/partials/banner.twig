{# ========================================================================== #}
{# :: Variables #}
{# ========================================================================== #}
{% set quote = section.title|default("") %}

{# ========================================================================== #}
{# :: Component #}
{# ========================================================================== #}
{% if quote %}
	<div class="c-marquee">
		<blockquote class="c-marquee__track">
			{{quote}}
			<span> •</span>
			{{quote}}
			<span> •</span>
			{{quote}}
			<span> •</span>
		</blockquote>
	</div>
{% endif %}